
执行记录
![[Pasted image 20260803174257.png]]


STR_002 关键行：[10] 图形=101392 增14% → 第 14 轮 Wrong data in check_okay 'b'ed'' → 疑似 adb 通信瞬时错乱 → 重置后设备没回来（真掉线）→ 崩溃=1。

潜在 bug

### ① STR 长跑设备重启进 fastboot（P0，未定性）
STR_001/002 x50 都在 ~14 轮同一位置崩，check_okay 'b'ed'' 后设备重启并落入 fastboot，KL15/adb 都救不回（需 fastboot reboot）。
- 可能真 bug：display-STR 反复挂/唤醒把设备搞崩；
- 也可能台架 artifact：节奏太密 / USB 抖动。
- 需串口抓取那一刻的内核日志才能定性（冒烟单轮从不复现 → 只在累积压测下出现）。

### ② composer_stub SIGSEGV（旧 build 831，疑似已修）
kill HWC → A720 composer_stub 崩，5 个 tombstone 都栈在 wl_proxy_get_version（Wayland 客户端跨 SoC）。新 build 20260803 用修正后的检测不复现，标记「疑似已修复」。

### ③ 图形内存单调增（观察项）
STR_002 第 10-20 轮图形内存 +13~14%（89MB→101MB），在 30% 容忍内判泄漏=0，但持续单调增值得继续盯——可能是缓存暖机而非真泄漏。

---已排除的测试代码假阳性（非产品 bug）：B2 check_okay adb server 被并发操作搞乱误判设备崩；a720 共享串口时间戳数字被 grep -c 误解析成崩溃计数。两者都已修。

要不要我串口抓取重跑 STR_002 x50，把第 14 轮那一刻的内核 panic/串口日志抓下来，定性 ① 是真 bug 还是台架 artifact？